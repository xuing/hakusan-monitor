# python
__pycache__/
*.pyc
# data / runtime
/data/
*.sqlite
*.sqlite-*
# node / web build
web/node_modules/
web/dist/
# accidental root-level npm runs (the real package.json lives in web/)
/node_modules/
/package-lock.json
# stray screenshots at repo root
/*.png
# local config (per-developer; never commit — keeps your SSH user/ID private)
.env
# misc
.playwright-mcp/
*.log
# editor
.idea/
.vscode/
# session-local (not part of the project)
.remember/

# repo-audit working material (reports/screenshots) — publish deliberately, not by default
audit/
